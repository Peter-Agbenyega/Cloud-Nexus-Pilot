name: Monorepo Deploy Placeholder

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Configure independent deployment jobs for apps/* inside CloudNexus-Pilot."
