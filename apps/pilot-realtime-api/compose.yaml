services:
  pilot-realtime-api:
    build:
      context: ../..
      dockerfile: apps/pilot-realtime-api/Dockerfile
    image: cloud-nexus-pilot-realtime:foundation
    container_name: cloud-nexus-pilot-realtime
    restart: unless-stopped
    init: true

    env_file:
      - .env

    # Keep the container port aligned with the Docker health check.
    environment:
      PORT: "3010"
      # Leave WS_TRUSTED_PROXY_CIDRS unset unless the only inbound path is a
      # deliberately configured local reverse proxy that overwrites
      # X-Forwarded-* headers before proxying to this loopback-bound port.
      # Example for a same-host proxy: WS_TRUSTED_PROXY_CIDRS: "127.0.0.1/32"

    ports:
      - "127.0.0.1:3010:3010"

    security_opt:
      - no-new-privileges:true

    cap_drop:
      - ALL

    read_only: true

    tmpfs:
      - /tmp:size=64m

    stop_grace_period: 15s
